<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testclassroom">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ListOfCommentsActivity"></activity>
        <activity android:name=".AboutUsActivity" />
        <activity android:name=".PersonProfileActivity" />
        <activity
            android:name=".CreateExamActivity"
            android:label="Create Exam" />
        <activity
            android:name=".AddTopicActivity"
            android:label="Customize Topic" />
        <activity
            android:name=".ProfileHomeworkActivity"
            android:label="Homework Profile" />
        <activity
            android:name=".ListOfHomeworkActivity"
            android:label="Assignments" />
        <activity
            android:name=".CreateHomeworkActivity"
            android:label="Create Homework" />
        <activity
            android:name=".ClassActivity"
            android:label="Class Profile" />
        <activity
            android:name=".NotificationActivity"
            android:label="Notifications" />
        <activity
            android:name=".ListOfClassActivity"
            android:label="Classes" />
        <activity
            android:name=".JoinClassActivity"
            android:label="Join Class" />
        <activity
            android:name=".CreateClassActivity"
            android:label="Class Activities" />
        <activity
            android:name=".SignInActivity"
            android:label="Sign In" />
        <activity
            android:name=".RegisterActivity"
            android:label="Sign Up" />
        <activity android:name=".WelcomeActivity" />
        <activity
            android:name=".MainActivity"
            android:label="Home"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>